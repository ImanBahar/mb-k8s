apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: microservices-dev

labels:
  - includeSelectors: true
    pairs:
      variant: dev

resources:
  - ../base

images:
  - name: backend-image-ref
    newName: busybox
    newTag: latest